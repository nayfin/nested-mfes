import { MfeOutletDirective } from './mfe-outlet.directive';

describe('MfeOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new MfeOutletDirective();
    expect(directive).toBeTruthy();
  });
});
